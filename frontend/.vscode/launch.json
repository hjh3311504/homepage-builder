{
  "configurations": [
    {
      "name": "chrome debugger",
      "request": "launch",
      "type": "chrome",
      "url": "https://localhost:5000",
      "webRoot": "${workspaceFolder}/"
    }
  ]
}
